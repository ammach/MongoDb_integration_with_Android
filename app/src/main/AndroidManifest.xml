<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ammach.mongodb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Simulation"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ComptesActivity"
            android:label="Liste des Comptes"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OperationsActivity"
            android:label="Liste des Opérations"
            android:parentActivityName=".ComptesActivity" />
    </application>

</manifest>